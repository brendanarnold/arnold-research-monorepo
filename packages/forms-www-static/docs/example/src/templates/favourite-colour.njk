<fieldset>
  <label for="{{ viewModel.name }}">Favourite colour</label>
  <input name="{{ viewModel.name }}" type="text" value="{{ viewModel.data }}" /> 
  <div id="{{ name }}__error-card" class="error-card hidden"></div>
</fieldset>